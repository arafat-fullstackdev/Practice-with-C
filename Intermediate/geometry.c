#include <stdio.h>

int main() {
    float pi = 3.14;
    int radius;
    float area;
   printf("Enter Radius: ");
    scanf("%d",&area);

    area = pi * radius *radius;

    printf("Area of Place:%2.f",area);

    return 0;
}
