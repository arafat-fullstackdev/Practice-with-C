#include <stdio.h>

int main() {
  int max,min;
  scanf("%d %d", &max, &min);
  if(max> main){
    printf("This is maximum");
  }  else if(max<min){
    printf("This is minimum");
  } else{
    return 0;
  }
    return 0;
}